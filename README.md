# EcoTracker Kotlin Android App

Simple Android app in Kotlin for the EcoTracker flow.

## Run

1. Open this folder in Android Studio.
2. Let Gradle sync.
3. Run the `app` configuration on an emulator or device.

Or from terminal (after Android SDK setup):

```bash
./gradlew installDebug
```
